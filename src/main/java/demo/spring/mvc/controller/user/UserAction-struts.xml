<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>

    <package name="user" namespace="/user" extends="frame">

        <action name="listUser" class="userAction" method="listUser">
            <result name="success">userList.ftl</result>
        </action>

        <action name="newUser" class="userAction" method="newUser">
            <result name="success">user.ftl</result>
        </action>

        <action name="loadUser" class="userAction" method="loadUser">
            <result name="success">user.ftl</result>
        </action>

        <action name="viewUser" class="userAction" method="viewUser">
            <result name="success">userView.ftl</result>
        </action>

        <action name="*User" class="userAction" method="{1}User">
            <result name="success" type="json">
                <param name="includeProperties">actionErrors.*, actionMessages.*</param>
                <param name="ignoreHierarchy">false</param>
            </result>
        </action>

    </package>

</struts>
